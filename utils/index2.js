const fs = require('fs');
// const markdown = require("markdown");

// -- test simple markdown
var markdown = require( "markdown" ).markdown;
console.log( markdown.toHTML( "Hello *World*!" ) );


// // --- 1. require ---- //
// const fs = require('fs');

// // -- 2. receive input
// var profileDataArgs = process.argv.slice(2, process.argv.length);

// // -- 3. display the data dynamically
// console.log(profileDataArgs);

// -- 4. collect command-line arguments in an array
// const profileDataArgs = process.argv.slice(2, process.argv.length);

// -- 5. feed them into the generatePage() function

// -- 6. create a string with the user input (
        // extract arguments, store in distict values


